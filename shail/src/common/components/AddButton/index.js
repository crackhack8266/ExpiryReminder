import React from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

const AddButton = () => {
  return <View style={styles.addButtonView}></View>;
};

export default AddButton;
